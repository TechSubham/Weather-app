services:
  frontend:
    build: ./frontend
    container_name: weather-frontend
    ports:
      - "3000:3000"
    environment:
      REACT_APP_OPENWEATHER_API_KEY: ${REACT_APP_OPENWEATHER_API_KEY:-}
